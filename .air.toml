# .air.toml
[build]
  cmd = "go run ./cmd/main.go"
  bin = "tmp/main"
  full_bin = "false"
  log = "build.log"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]

[log]
  time = true

[color]
  main = "yellow"
  watcher = "cyan"
  build = "green"
  runner = "magenta"